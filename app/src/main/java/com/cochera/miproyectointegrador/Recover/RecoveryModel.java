package com.cochera.miproyectointegrador.Recover;

import android.content.Context;

import com.cochera.miproyectointegrador.DataBase.DBHelper;

import java.util.Random;
public class RecoveryModel {
    /*
    private DBHelper dbHelper;
    private String generatedCode;

    public RecoveryModel(Context context) {
        this.dbHelper = new DBHelper(context);
    }

    public interface RecoveryCallback {
        void onSuccess();
        void onFailure(String error);
    }





    public void updatePassword(String email, String newPassword, RecoveryCallback callback) {
        if (dbHelper.actualizarContrasena(email, newPassword)) {
            callback.onSuccess();
        } else {
            callback.onFailure("Error al actualizar la contraseña");
        }
    }
*/
}
